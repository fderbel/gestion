
server: 
  port: 8081
spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  #mvc:
    #pathmatch:
       #matching-strategy: ANT_PATH_MATCHER
  datasource:
    url: jdbc:postgresql://localhost:5434/gestiondestock?createDatabaseIfNotExiste=true
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver
    
  springdoc:
    swagger-ui:
      path: /swagger-ui.html

  #springdoc:
  #  swagger-ui:
   #   disable-swagger-default-url: true
   # api-docs:
    #    path: /javainuse-openapi