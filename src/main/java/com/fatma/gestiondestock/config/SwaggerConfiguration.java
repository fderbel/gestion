package com.fatma.gestiondestock.config;

/*
 * @EnableSwagger2
 * 
 * @Configuration public class SwaggerConfiguration {
 * 
 * // @Bean
 * 
 * public Docket api() {
 * 
 * return new Docket(DocumentationType.SWAGGER_2)
 * 
 * .apiInfo(new
 * ApiInfoBuilder().description("gestion de stock").title("gestion de stock api"
 * ).build()) .groupName("REST API V1").select()
 * .apis(RequestHandlerSelectors.basePackage(
 * "com.fatma.gestiondestock.controller"))
 * .paths(PathSelectors.ant("/v1/gestionStock" + "/**")).build(); }
 * 
 * }
 */
